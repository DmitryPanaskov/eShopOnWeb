name: Trigger auto deployment for public-api-dp-container

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ develop ]
    paths:
    - '//**'
    - '.github/workflows/public-api-dp-container-AutoDeployTrigger-b958394e-4766-43c7-8c1f-0cc5670e701f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.PUBLICAPIDPCONTAINER_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.PUBLICAPIDPCONTAINER_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.PUBLICAPIDPCONTAINER_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: azurecontainerregisterdp.azurecr.io
          registryUsername: ${{ secrets.PUBLICAPIDPCONTAINER_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.PUBLICAPIDPCONTAINER_REGISTRY_PASSWORD }}
          containerAppName: public-api-dp-container
          resourceGroup: Learn
          imageToBuild: azurecontainerregisterdp.azurecr.io/public-api-dp-container:${{ github.sha }}
          dockerfilePath: Dockerfile


